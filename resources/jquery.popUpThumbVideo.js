/**
* Simple script to add pop-up video dialog link support for video thumbnails 
*/
( function( mw, $ ) {
	
	$.fn.popUpVideoLink( options ){
		$j(this).click(function(){
			// Spawn a new dialog window with the video asset
			
		})
	}

} )( mediaWiki, jQuery );